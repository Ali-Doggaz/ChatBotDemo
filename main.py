from llama_index import SimpleDirectoryReader, GPTListIndex, GPTVectorStoreIndex, llm_predictor, PromptHelper
from langchain import OpenAI
import sys
import os

# Press the green button in the gutter to run the script.
if __name__ == '__main__':


